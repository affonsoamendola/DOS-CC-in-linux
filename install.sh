#!/bin/bash

echo "Remember to change both files to reflect your installation of the compiler, fix the include and library folder in tcc, and the mount point and PATH in the bottom of tcc.conf"

sudo cp tcc /usr/bin/
cp tcc.conf ~/.dosbox/
